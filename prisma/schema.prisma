generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Josaa2025 {
  sno                 Decimal  @id(map: "Josaa2025_pkey") @db.Decimal
  round               Decimal? @db.Decimal
  institute           String?
  academicProgramName String?
  quota               String?
  seatType            String?
  gender              String?
  openingRank         String?
  closingRank         String?
  priority            Int?
}
